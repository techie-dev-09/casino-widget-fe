import React from "react";

function Divider() {
  return (
    <span
      style={{
        mixBlendMode: "normal",
        borderRadius: "1px",
        opacity: 0.38,
        borderLeft: "1px solid #000",
        margin: "10px 10px 10px 0px",
      }}
    ></span>
  );
}

export default Divider;
